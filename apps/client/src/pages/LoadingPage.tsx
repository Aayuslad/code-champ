const LoadingPage = () => {
    return (
        <div className="w-screen h-screen flex flex-col items-center justify-center dark:text-white dark:bg-dark100">
            <h2>Loading...</h2>
            <p>Please wait while we load the content.</p>
        </div>
    );
};

export default LoadingPage;
