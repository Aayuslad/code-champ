{
	"version": 2,
	"builds": [
		{
			"src": "apps/server/src/**/*.ts",
			"use": "@vercel/node"
		}
	],
	"installCommand": "npm install && npm run build --workspace=@repo/common",
	"routes": [
		{
			"src": "/(.*)",
			"dest": "apps/server/src/index.ts"
		}
	]
}
